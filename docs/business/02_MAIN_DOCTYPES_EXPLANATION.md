# 📘 المستند رقم (2): قاموس البيانات والمنطق الوظيفي
**المشروع:** Memora Gamified LMS
**الهدف:** شرح الوظيفة الدقيقة لكل DocType وكل Field في النظام، وكيفية تأثيرها على البزنس لوجيك.

---

## 1. البيانات الأكاديمية (البنية التحتية)
تستخدم هذه الجداول لتعريف "هوية الطالب" وتوجيهه للمحتوى الصحيح.

### 🏛️ `Game Academic Grade` (الصف الدراسي)
*   **الوصف:** يمثل السنة الدراسية (مثل: التوجيهي، الصف العاشر).
*   **شرح الحقول:**
    *   **`grade_name`**: الاسم الظاهر للطالب في شاشة التسجيل (Onboarding).
    *   **`valid_streams`**: (Table) قائمة التخصصات المسموحة لهذا الصف.
        *   *المنطق:* يستخدم في الـ Frontend لفلترة القائمة المنسدلة للتخصصات. إذا اختار الطالب "صف عاشر"، وهذا الجدول فارغ، لن يطلب منه النظام اختيار تخصص.

### 🧬 `Game Academic Stream` (التخصص/الفرع)
*   **الوصف:** يمثل المسار الأكاديمي (علمي، أدبي، صناعي).
*   **شرح الحقول:**
    *   **`stream_name`**: الاسم التعريفي.

### 📅 `Game Subscription Season` (الموسم الدراسي)
*   **الوصف:** يمثل الوعاء الزمني للاشتراكات والخطط (مثل: "Tawjihi 2025").
*   **شرح الحقول:**
    *   **`start_date` / `end_date`**: تحدد صلاحية أي اشتراك مرتبط بهذا الموسم.
        *   *المنطق:* عند فحص صلاحية اشتراك الطالب، لا ننظر لتاريخ الشراء، بل ننظر لـ `Season.end_date`. إذا انتهى الموسم، تنتهي صلاحية آلاف الطلاب فوراً.
    *   **`is_active`**: يحدد الموسم الافتراضي الذي سيتم تعيينه للطلاب الجدد عند التسجيل.

---

## 2. التخطيط الأكاديمي (العقل المدبر) 🧠
هنا يتم ربط الطالب بالمحتوى بشكل ديناميكي.

### 🗺️ `Game Academic Plan` (الخطة الدراسية)
*   **الوصف:** يربط (صف + تخصص + موسم) بمجموعة مواد محددة.
*   **شرح الحقول:**
    *   **`grade` + `stream` + `season`**: المفتاح المركب للخطة (Unique Combination).
    *   **`subjects`**: جدول المواد التي يدرسها هذا التخصص.

#### 📄 `Game Plan Subject` (تفاصيل المادة في الخطة)
*   **شرح الحقول:**
    *   **`subject`**: رابط للمادة الأصلية.
    *   **`display_name`**: (اختياري) لتغيير اسم المادة لهذا التخصص فقط (مثلاً: "رياضيات عامة" بدلاً من "رياضيات").
    *   **`selection_type`**:
        *   `All Units`: الطالب يرى كل محتوى المادة.
        *   `Specific Unit`: الطالب يرى وحدات محددة فقط (حالة الحذف أو المواد المشتركة جزئياً).
    *   **`specific_unit`**: يحدد الوحدة المطلوبة (في حال اخترنا Specific Unit).

---

## 3. هيكلية المحتوى (Content Hierarchy) 📚

### 📦 `Game Subject` (المادة)
*   **الوصف:** الحاوية الرئيسية للمحتوى (مثل: الرياضيات).
*   **شرح الحقول:**
    *   **`is_paid`**: القفل الرئيسي.
        *   `True`: المادة مغلقة بالكامل (إلا ما استثني).
        *   `False`: المادة مفتوحة للجميع مجاناً.

### 🛤️ `Game Learning Track` (المسار التعليمي)
*   **الوصف:** تقسيم وظيفي للمادة (منهاج، تأسيس، مكثف).
*   **شرح الحقول:**
    *   **`is_paid`**: هل يحتاج هذا المسار لاشتراك؟
        *   اذا كانت المادة مدفوعة، وهذا `False`، يصبح المسار مجانياً للجميع (طعم تسويقي).
    *   **`is_sold_separately`**: هل يباع لوحده؟
        *   `True`: اشتراك المادة العادي **لا يفتح** هذا المسار (منتج Premium منفصل).
        *   `False`: اشتراك المادة يفتحه تلقائياً.

### 📖 `Game Unit` (الوحدة)
*   **الوصف:** الفصل الدراسي.
*   **شرح الحقول:**
    *   **`structure_type`**: يحدد طريقة عرض الوحدة في الخريطة:
        *   `Topic Based`: تعرض كـ "كواكب" (Clusters).
        *   `Lesson Based`: تعرض كـ "مسار متصل" (Path).
    *   **`is_free_preview`**: إذا `True`، تفتح الوحدة مجاناً متجاوزة كل شروط الدفع (للتجربة).

### 🪐 `Game Topic` (الموضوع/الكوكب)
*   **الوصف:** تجميع لعدة دروس تحت عنوان واحد.
*   **شرح الحقول:**
    *   **`is_linear`**: إذا `True`، لا يمكن للطالب فتح الدرس رقم 2 إلا بعد إنهاء الدرس رقم 1.
    *   **`is_free_preview`**: يسمح بجعل "موضوع واحد" مجانياً داخل وحدة مدفوعة.

### 🎮 `Game Lesson` (الدرس)
*   **الوصف:** الوحدة القابلة للعب.
*   **شرح الحقول:**
    *   **`stages`**: يحتوي على الـ JSON الخاص باللعبة (الأسئلة، الإجابات، الصور).

---

## 4. الاقتصاد والمتجر (Economy) 🛒

### 🛍️ `Game Sales Item` (المنتج/الباقة)
*   **الوصف:** العنصر الذي يظهر في صفحة المتجر.
*   **شرح الحقول:**
    *   **`linked_season`**: يحدد تاريخ انتهاء صلاحية هذا المنتج (يجب أن يطابق موسم الطالب).
    *   **`target_grade` / `target_streams`**: فلاتر العرض.
        *   المنتج لا يظهر في المتجر إلا إذا طابق صف وتخصص الطالب (لمنع الخطأ في الشراء).
    *   **`bundle_contents`**: قائمة الصلاحيات التي يمنحها هذا المنتج (مادة، تراك، إلخ).

### 🎫 `Game Player Subscription` (الاشتراك الفعال)
*   **الوصف:** السجل الذي يثبت حق الطالب في الوصول.
*   **شرح الحقول:**
    *   **`linked_season`**: المرجع الزمني للصلاحية.
    *   **`access_items`**: قائمة ما يملكه الطالب.
    *   **`type`**: `Global` (يفتح كل شيء) أو `Specific` (يفتح عناصر الجدول فقط).

### 📝 `Game Purchase Request` (طلب الشراء)
*   **الوصف:** نقطة الانتظار (Queue) قبل التفعيل، لأننا نستخدم الدفع اليدوي/الحوالات.
*   **المنطق:** عند تحويل الحالة إلى `Approved`، يقوم النظام تلقائياً بإنشاء `Subscription`.

---

## 5. بيانات اللاعب والتقدم (User Data) 👤

### 👤 `Player Profile` (ملف الطالب)
*   **شرح الحقول:**
    *   **`current_grade` / `stream`**: تحدد الخريطة التي يراها الطالب.
    *   **`academic_year`**: يربطه بموسم محدد (لمنع اختلاط خطط السنوات السابقة).

### 🧠 `Player Memory Tracker` (الذاكرة - SRS)
*   **الوصف:** يتتبع كل معلومة (سؤال) تعلمها الطالب.
*   **شرح الحقول:**
    *   **`subject`**: المادة التابع لها السؤال.
    *   **`topic`**: التوبيك التابع له السؤال (يستخدم لفلترة "مراجعة التوبيك").
    *   **`stability`**: رقم من 1-4 يحدد قوة الحفظ (1=ضعيف، 4=راسخ).
    *   **`next_review_date`**: متى يجب أن يظهر هذا السؤال مرة أخرى في "المراجعة اليومية".
